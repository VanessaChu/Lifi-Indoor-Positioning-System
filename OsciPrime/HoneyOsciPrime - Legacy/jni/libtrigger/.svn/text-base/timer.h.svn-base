#include <string>
class Timer{
	public:
		Timer(std::string ctx, bool debug);
		void start();
		double stop();
		double getTime();
	private:
		std::string ctx;
		bool debug;
		double startTime;
		double stopTime;
};

